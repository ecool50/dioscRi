% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modeling_and_visualization.R
\name{fitModel}
\alias{fitModel}
\title{Fit Overlapping Group Lasso Model}
\usage{
fitModel(
  xTrain,
  yTrain,
  groups,
  alpha = NULL,
  lambda = NULL,
  alphaSearch = seq(0.01, 0.1, 0.01),
  penalty = "cMCP",
  modelSummary = FALSE,
  seed = 1994
)
}
\arguments{
\item{xTrain}{Matrix of training features.}

\item{yTrain}{Vector of training labels.}

\item{groups}{List of groups for overlapping group lasso.}

\item{alpha}{Numeric; alpha value for regularization.}

\item{alphaSearch}{Numeric vector of alpha values to search, if alpha is NULL.}

\item{penalty}{Character; penalty type, default is "cMCP".}

\item{modelSummary}{Logical; should the model summary be printed}

\item{seed}{Integer; random seed for reproducibility.}
}
\value{
List containing the fitted model.
}
\description{
Fits the group lasso model to data with the selected alpha. If alpha is not provided,
searches for optimal alpha using BIC.
}
\examples{
# Load sample data
data(sample_data_logit)
data(sample_markerMeans)
data(sample_groups)
data(sample_clinicaldata)

# Prepare training data
X_train <- cbind(sample_data_logit, sample_markerMeans)
X_train <- scale(X_train)
y_train <- as.numeric(sample_clinicaldata$Outcome) - 1

# Fit with automatic parameter selection
fit_auto <- fitModel(
  xTrain = X_train,
  yTrain = y_train,
  groups = sample_groups,
  penalty = "grLasso",
  seed = 1994
)

# Check coefficients
coefs <- coef(fit_auto$fit)
sum(coefs != 0)
}
