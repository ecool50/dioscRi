% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MMD_Vae.R
\name{computeSlicedBarycenter}
\alias{computeSlicedBarycenter}
\title{Compute Sliced Wasserstein Barycenter}
\usage{
computeSlicedBarycenter(
  samplesList,
  nCellsPerSample = 2000L,
  nBarycenterCells = 5000L,
  nProjections = 100L,
  nIter = 10L,
  seed = 1994
)
}
\arguments{
\item{samplesList}{List of data frames or matrices, one per sample.
Each element contains cells (rows) x markers (columns).}

\item{nCellsPerSample}{Integer; number of cells to subsample from each sample.
Default is 2000.}

\item{nBarycenterCells}{Integer; number of cells in the output barycenter.
Default is 5000.}

\item{nProjections}{Integer; number of random projections per iteration.
Default is 100.}

\item{nIter}{Integer; number of barycenter iterations. Default is 10.}

\item{seed}{Integer; random seed. Default is 1994.}
}
\value{
Matrix of barycenter cells (nBarycenterCells x markers).
}
\description{
Computes the Wasserstein barycenter of multiple samples using the sliced
Wasserstein distance. The barycenter is a representative distribution that
minimizes the average transport cost to all input samples. This is used as
the reference distribution for OT batch correction.
}
\examples{
\dontrun{
# Split decoded data by sample
decoded_df <- as.data.frame(decoded_data)
decoded_df$sample_id <- df$sample_id
samples_list <- split(decoded_df[, markers], decoded_df$sample_id)

# Compute barycenter
barycenter <- computeSlicedBarycenter(
  samplesList = samples_list,
  nCellsPerSample = 10000,
  nBarycenterCells = 5000,
  nProjections = 200,
  nIter = 20
)
}
}
