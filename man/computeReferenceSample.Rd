% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_processing.R
\name{computeReferenceSample}
\alias{computeReferenceSample}
\title{Compute Reference Sample Based on Covariance Norms}
\usage{
computeReferenceSample(data, markers, sampleCol = "sample_id", N = 2)
}
\arguments{
\item{data}{Data frame containing sample information.}

\item{markers}{Character vector of marker column names.}

\item{sampleCol}{Character; name of the sample ID column.}

\item{N}{Integer; number of top and bottom samples to return.}
}
\value{
A list containing the matrix of norms, average norms, and sample indices
for top, bottom, median, reference, and validation samples.
}
\description{
This function computes covariance matrices for each sample, then calculates
pairwise Frobenius norms between them to identify reference samples.
Returns the top, bottom, median, reference, and validation samples.
}
\examples{
# Load sample data
data(sample_cytof_data)
data(sample_markers)

# Find reference samples based on covariance structure
ref_samples <- computeReferenceSample(
  data = sample_cytof_data,
  markers = sample_markers,
  sampleCol = "sample_id",
  N = 2
)

print(ref_samples$refSampleInd)
print(ref_samples$topNSamples)
print(ref_samples$bottomNSamples)
}
