% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MMD_Vae.R
\name{trainSWAEClusteringFast}
\alias{trainSWAEClusteringFast}
\title{Train SWAE-Clustering (Fast Version)}
\usage{
trainSWAEClusteringFast(
  trainData,
  useMarkers,
  nClusters = 10L,
  pseudoLabels = NULL,
  epochs = 100L,
  latentDim = 16L,
  lambda = 0.1,
  lambdaCluster = 1,
  numProjections = 50L,
  batchSize = 32L,
  learningRate = 0.001,
  hiddenSizes = c(23L, 19L),
  clusterWarmup = 10L,
  jointFineTune = FALSE,
  jointEpochs = 30L,
  labelSmoothing = 0,
  dropout = 0,
  useBatchNorm = FALSE,
  useLayerNorm = FALSE,
  weightDecay = 0,
  useDSW = FALSE,
  dswMaxIter = 10L,
  dswLambda = 1,
  verbose = 1L,
  seed = 1994
)
}
\arguments{
\item{trainData}{A data frame or matrix containing the training data.}

\item{useMarkers}{Character vector of column names to use as input features.}

\item{nClusters}{Integer, number of clusters (default 10).}

\item{pseudoLabels}{Optional vector of pseudo-labels for supervised training.}

\item{epochs}{Integer, total number of training epochs (default 100).}

\item{latentDim}{Integer, dimensionality of the latent space (default 16).}

\item{lambda}{Numeric, weight for Sliced Wasserstein distance loss (default 0.1).}

\item{lambdaCluster}{Numeric, weight for clustering loss (default 1.0).}

\item{numProjections}{Integer, number of random projections for SW (default 50).}

\item{batchSize}{Integer, training batch size (default 32).}

\item{learningRate}{Numeric, learning rate for optimizer (default 1e-3).}

\item{hiddenSizes}{Integer vector of length 2, hidden layer sizes (default c(23, 19)).}

\item{clusterWarmup}{Integer, epochs for Stage 1 SWAE warmup (default 10).}

\item{jointFineTune}{Logical, whether to do Stage 3 joint fine-tuning (default FALSE).}

\item{jointEpochs}{Integer, number of Stage 3 epochs (default 30).}

\item{labelSmoothing}{Numeric, label smoothing factor (default 0.0).}

\item{dropout}{Numeric, dropout rate (default 0.0).}

\item{useBatchNorm}{Logical, use batch normalization (default FALSE).}

\item{useLayerNorm}{Logical, use layer normalization (default FALSE).}

\item{weightDecay}{Numeric, weight decay for AdamW (default 0.0).}

\item{useDSW}{Logical, use Distributional Sliced Wasserstein (default FALSE).}

\item{dswMaxIter}{Integer, DSW inner optimization steps (default 10).}

\item{dswLambda}{Numeric, DSW diversity regularization strength (default 1.0).}

\item{verbose}{Integer, verbosity level (default 1).}

\item{seed}{Integer, random seed (default 1994).}
}
\value{
A list containing trained encoder, decoder, clusterHead models, and training history.
}
\description{
A faster alternative to \code{\link{trainSWAEClustering}} that eliminates
per-batch R-to-TF tensor conversions. Uses the same R-level training loop
and shuffling as the original but pre-converts all data to TF tensors once
and uses TF gather for batching. Supports the core SWAE + DSW + supervised
clustering pipeline.
}
\examples{
\dontrun{
model <- trainSWAEClusteringFast(trainData, useMarkers, nClusters = 11,
  pseudoLabels = labels, epochs = 100, latentDim = 16,
  hiddenSizes = c(23, 19), clusterWarmup = 20,
  useDSW = TRUE, dswMaxIter = 10, dswLambda = 1.0)
}
}
