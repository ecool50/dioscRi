% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MMD_Vae.R
\name{computeSlicedBarycenterAutograd}
\alias{computeSlicedBarycenterAutograd}
\title{Compute Sliced Wasserstein Barycenter via Autograd}
\usage{
computeSlicedBarycenterAutograd(
  samplesList,
  nCellsPerSample = 2000L,
  nBarycenterCells = 5000L,
  nProjections = 50L,
  nIter = 200L,
  learningRate = 0.01,
  sampleBatchSize = 16L,
  seed = 1994
)
}
\arguments{
\item{samplesList}{List of data frames or matrices, one per sample. Each
element has cells as rows and markers as columns.}

\item{nCellsPerSample}{Integer; number of cells to subsample from each
distribution. Default is 2000.}

\item{nBarycenterCells}{Integer; number of cells in the barycenter. Default
is 5000.}

\item{nProjections}{Integer; number of random projections per gradient step.
Default is 50.}

\item{nIter}{Integer; number of gradient descent iterations. Default is 200.}

\item{learningRate}{Numeric; Adam learning rate. Default is 0.01.}

\item{sampleBatchSize}{Integer; number of samples per mini-batch. Processes
samples in chunks to avoid OOM. Default is 16.}

\item{seed}{Integer; random seed. Default is 1994.}
}
\value{
Matrix of barycenter cells (nBarycenterCells x markers).
}
\description{
Computes the barycenter (Frechet mean) of multiple empirical distributions
using gradient descent on the sliced Wasserstein-2 distance, with TensorFlow
automatic differentiation. This is a more principled alternative to the
fixed-point iteration in \code{\link{computeSlicedBarycenter}}.
}
\examples{
\dontrun{
barycenter <- computeSlicedBarycenterAutograd(
  samplesList = samples_list,
  nCellsPerSample = 10000,
  nBarycenterCells = 5000,
  nProjections = 50,
  nIter = 200
)
}
}
