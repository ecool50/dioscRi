% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MMD_Vae.R
\name{computeCellImportance}
\alias{computeCellImportance}
\title{Compute Cell Importance Scores}
\usage{
computeCellImportance(
  data,
  encoder,
  decoder,
  method = "recon_error",
  clusterHead = NULL,
  temperature = 1,
  normalize = FALSE
)
}
\arguments{
\item{data}{Matrix of marker expression data (cells x markers).}

\item{encoder}{Trained encoder model from trainSWAEClustering.}

\item{decoder}{Trained decoder model from trainSWAEClustering.}

\item{method}{Method for computing importance: "recon_error" (inverse reconstruction error),
"cluster_confidence" (max cluster probability), or "combined" (product of both).}

\item{clusterHead}{Optional cluster head model for cluster_confidence method.}

\item{temperature}{Temperature for softmax conversion of scores. Lower = more peaked.}

\item{normalize}{If TRUE, normalize weights to sum to 1 within each sample.}
}
\value{
Numeric vector of importance weights for each cell.
}
\description{
Computes importance weights for each cell based on reconstruction quality.
Cells with lower reconstruction error are considered more "representative"
and receive higher importance weights. This can be used for attention-weighted
feature aggregation.
}
\examples{
\dontrun{
model <- trainSWAEClustering(trainData, useMarkers, nClusters = 10)
importance <- computeCellImportance(
  data = allData[, useMarkers],
  encoder = model$encoder,
  decoder = model$decoder,
  method = "recon_error"
)
}
}
