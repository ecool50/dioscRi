% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MMD_Vae.R
\name{trainSWAEEnhanced}
\alias{trainSWAEEnhanced}
\title{Train Enhanced Sliced Wasserstein Autoencoder (SWAE) with Annealing}
\usage{
trainSWAEEnhanced(
  trainData,
  useMarkers,
  epochs = 100L,
  latentDim = 32L,
  lambda = 10,
  lambdaAnnealEpochs = 0L,
  numProjections = 100L,
  batchSize = 256L,
  learningRate = 0.001,
  hiddenDims = c(256L, 128L),
  valData = NULL,
  verbose = 10L,
  seed = 1994
)
}
\arguments{
\item{trainData}{Data frame or matrix of training data.}

\item{useMarkers}{Character vector; names of marker columns to use.}

\item{epochs}{Integer; number of training epochs, default is 100.}

\item{latentDim}{Integer; dimensionality of latent space. Default is 32.}

\item{lambda}{Numeric; final regularization weight for SW loss, default is 10.0.}

\item{lambdaAnnealEpochs}{Integer; epochs to linearly anneal lambda from 0 to final value.
Default is 0 (no annealing). Recommended: 20-50 epochs for better representations.}

\item{numProjections}{Integer; number of random projections for SW distance, default is 100L.}

\item{batchSize}{Integer; batch size for training, default is 256.}

\item{learningRate}{Numeric; learning rate, default is 1e-3.}

\item{hiddenDims}{Integer vector; sizes of hidden layers. Default is c(256, 128).}

\item{valData}{Optional validation data for monitoring.}

\item{verbose}{Integer; print frequency. Default is 10 (every 10 epochs).}

\item{seed}{Integer; random seed for reproducibility, default is 1994.}
}
\value{
List containing encoder, decoder, and training history.
}
\description{
Trains an autoencoder with Sliced Wasserstein Distance regularization using
a custom training loop that supports lambda annealing. This version offers
better control over training dynamics and typically produces better latent
representations than the standard SWAE.
}
\examples{
\dontrun{
# Train enhanced SWAE with lambda annealing
swae_model <- trainSWAEEnhanced(
  trainData = train_data,
  useMarkers = markers,
  epochs = 100,
  latentDim = 32,
  lambda = 10.0,
  lambdaAnnealEpochs = 30,  # Anneal over first 30 epochs
  hiddenDims = c(256, 128),
  batchSize = 256,
  seed = 1994
)

# Get latent representations
latent <- encodeSWAE(swae_model, new_data)
}
}
